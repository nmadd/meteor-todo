import { Meteor } from 'meteor/meteor';
import Todos from '../imports/collections/Todos';

// launch server and add any config options
Meteor.startup(() => {
  // code to run on server at startup

});
